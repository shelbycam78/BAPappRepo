@model IEnumerable<BAPapp.Models.Venue.VenueListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VenueName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VenueLocation)
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VenueName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VenueLocation)
        </td>
       
        <td>
            @Html.ActionLink("Edit", "Edit", new {VenueName = item.VenueName }) |
            @Html.ActionLink("Details", "Details", new { VenueName = item.VenueName }) |
            @Html.ActionLink("Delete", "Delete", new { VenueName = item.VenueName })
        </td>
    </tr>
}

</table>
